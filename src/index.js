import React from 'react';
import { createRoot } from 'react-dom/client';
import App from './App';
import './index.css'; // Optional: include if you create this file

createRoot(document.getElementById('root')).render(<App />);
